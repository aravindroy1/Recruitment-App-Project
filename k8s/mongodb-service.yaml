apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: recruitment-app
  labels:
    app: mongodb

spec:
  type: ClusterIP

  selector:
    app: mongodb

  ports:
  - name: mongo-port
    port: 27017
    targetPort: 27017
